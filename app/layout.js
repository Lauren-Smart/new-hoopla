export const metadata = {
  title: "Smart Commercial Energy — Wins Board",
  description: "Live sales and delivery celebrations",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
